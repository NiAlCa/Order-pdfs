{
  "name": "orderpdfs",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "rebuild": "electron-rebuild -f -w canvas",
    "build": "electron-packager . my-electron-app --platform=win32 --arch=x64 --overwrite --out=dist"
  },
  "author": "NiAlCa",
  "license": "ISC",
  "devDependencies": {
    "electron": "12.2.3",
    "electron-builder": "^24.12.0",
    "electron-rebuild": "^3.2.9"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "pdf2pic": "^3.1.1",
    "tesseract.js": "^5.0.4"
  },
  "build": {
    "npmRebuild": false
  }
}
